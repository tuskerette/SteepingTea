<%= form_for @song do |f| %>
  <%= f.label :url %>
  <%= f.text_field :url %>

  <%= f.label :comment %>
  <%= f.text_field :comment %>

  <%= f.submit %>
<% end %>

<%= link_to "Back to all songs", songs_path %>
